apiVersion: v1
kind: Service
metadata:
  name: {{ include "postchart.fullname" . }}
  labels:
    {{- include "postchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postchart.selectorLabels" . | nindent 4 }}
